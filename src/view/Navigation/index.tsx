import { Box, Button } from '@mui/material';

export default function Navigation() {
  return (
    <Box>
        <Button>로그인</Button>
        <Button>회원가입</Button>
        <Button>작성하기</Button>
    </Box>
  )
}
